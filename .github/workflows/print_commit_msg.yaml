name: Print Message

on:
  push:
    branches:
      - main

jobs:
  print-message:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print commit message
        run: |
          echo "Commit message:"
          git log -1 --pretty=%B